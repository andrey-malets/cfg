[global]
    security     = ads
    realm        = RUNC.URGU.ORG
    workgroup    = RUNC
    netbios name = <%= @hostname %>
    log file     = /var/log/samba/thelog
    max log size = 0

;     vfs objects = acl_xattr
;     map acl inherit = yes
;     store dos attributes = yes
;
;     template shell = /bin/bash
;
    load printers         = no
    printing              = bsd
    printcap name         = /dev/null
;    show add print wizard = no
    disable spoolss       = yes

[place]
    path           = /place
    ; nt acl support = yes
    read only      = no
